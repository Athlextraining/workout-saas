# DEC Chain Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a git-native DEC (Decision) chain — one `DECISIONS.md` file plus four user-invocable skills — so incoming collaborators can record decisions, track status, and assign ownership.

**Architecture:** A single root `DECISIONS.md` holds an auto-maintained status-board table followed by one detailed block per DEC. A root `contributors.md` table is the source of truth for valid assignees. Four prose skills under `.claude/skills/` (`dec-creation`, `dec-amend`, `dec-deletion`, `dec-close`) read/mutate `DECISIONS.md` following fixed rules.

**Tech Stack:** Markdown only. Skills are prose instruction files (`SKILL.md` with YAML frontmatter), matching the existing `.claude/skills/generate-template-cycle/SKILL.md` convention. Git for branch/merge state.

## Global Constraints

- Skill frontmatter MUST include `name`, `description`, `user_invocable: true` — copy the existing `generate-template-cycle` convention exactly.
- DEC codes are zero-padded `DEC-NNN`, next = `max(all existing)+1`, **never reused or renumbered**.
- Six statuses only: `Pending`, `In Progress`, `Blocked`, `Done`, `Rejected`, `Superseded`.
- Assignee is a single `@handle` validated against `contributors.md`, or `Unassigned`.
- Branch convention: `dec-NNN-slug`.
- Every skill that mutates the chain MUST regenerate the status-board table.
- Commit style: Conventional Commits, subject only, **no `Co-Authored-By` / AI trailer** (project rule in AGENTS.md).
- Placeholder/example names must be neutral/professional (user global style rule).

---

## File Structure

- `contributors.md` (create) — assignee table.
- `DECISIONS.md` (create) — chain file: format legend + status board + DEC blocks. Self-documents the format.
- `.claude/skills/dec-creation/SKILL.md` (create) — create a DEC, contradiction check.
- `.claude/skills/dec-amend/SKILL.md` (create) — edit an existing DEC.
- `.claude/skills/dec-deletion/SKILL.md` (create) — set a DEC to Rejected.
- `.claude/skills/dec-close/SKILL.md` (create) — close a DEC on merge to main.

Each file has one responsibility; the four skills are independent and can be reviewed/rejected separately.

---

### Task 1: `contributors.md`

**Files:**
- Create: `contributors.md`

**Interfaces:**
- Produces: the assignee source of truth. Skills read `@handle` values from the first column of the `| GitHub | Name | Role |` table. `Unassigned` is always valid and is NOT listed here.

- [ ] **Step 1: Write the file**

```markdown
# Contributors

Source of truth for DEC assignees. Add a row before assigning someone a DEC.
Use the exact GitHub handle (with leading `@`). `Unassigned` is always valid and
is not listed here.

| GitHub  | Name            | Role       |
|---------|-----------------|------------|
| @patri  | Patricio Guerra | Maintainer |
```

- [ ] **Step 2: Verify structure**

Run: `grep -E '^\| @' contributors.md`
Expected: at least one row beginning `| @` (the maintainer row).

- [ ] **Step 3: Commit**

```bash
git add contributors.md
git commit -m "docs: add contributors table for DEC assignments"
```

---

### Task 2: `DECISIONS.md` scaffold

**Files:**
- Create: `DECISIONS.md`

**Interfaces:**
- Produces: the chain file. Skills depend on these exact anchors and formats:
  - A status-board table directly under the `## Status board` heading with columns `| DEC | Title | Status | Assignee |`.
  - DEC blocks start with `## DEC-NNN — Title` and contain the metadata lines `- Status:`, `- Assignee:`, `- Created:`, `- Branch:`, `- Supersedes:`, `- Superseded by:`, `- Related:`, then `### Context`, `### Decision`, `### Tasks` (GitHub checkboxes).
- This scaffold also includes `DEC-001` as the bootstrap decision (adopting the DEC chain itself), so the file is never empty and serves as a worked example.

- [ ] **Step 1: Write the file**

```markdown
# Decisions (DEC chain)

This file records project decisions and the work they spawn. Each **DEC** is one
numbered record with a single owner and a status. Manage it with the skills
`/dec-creation`, `/dec-amend`, `/dec-deletion`, `/dec-close` — avoid editing by
hand so the status board stays consistent.

**Statuses:** `Pending` · `In Progress` · `Blocked` · `Done` · `Rejected` · `Superseded`

**Rules:** codes are zero-padded and never reused or renumbered; assignee is a
single `@handle` from `contributors.md` (or `Unassigned`); a contradicting new
DEC supersedes the old one (bidirectional link).

## Status board

| DEC     | Title                  | Status | Assignee |
|---------|------------------------|--------|----------|
| DEC-001 | Adopt the DEC chain    | Done   | @patri   |

---

## DEC-001 — Adopt the DEC chain

- Status: Done
- Assignee: @patri
- Created: 2026-06-20
- Branch: —
- Supersedes: —
- Superseded by: —
- Related: —

### Context

The repository is opening to outside collaborators and needs a lightweight,
git-native way to record decisions, track their status, and assign ownership.

### Decision

Adopt a single-file DEC chain (`DECISIONS.md`) plus a `contributors.md` table and
four management skills. See `docs/superpowers/specs/2026-06-20-dec-chain-design.md`.

### Tasks

- [x] Define the on-disk format
- [x] Seed the contributors table
- [x] Provide the management skills
```

- [ ] **Step 2: Verify anchors exist**

Run: `grep -nE '^## Status board$|^## DEC-001 — |^- Status: |^### Tasks$' DECISIONS.md`
Expected: all four anchor lines present.

- [ ] **Step 3: Commit**

```bash
git add DECISIONS.md
git commit -m "docs: scaffold DECISIONS.md chain with bootstrap DEC-001"
```

---

### Task 3: `/dec-creation` skill

**Files:**
- Create: `.claude/skills/dec-creation/SKILL.md`

**Interfaces:**
- Consumes: `DECISIONS.md` format and `contributors.md` table from Tasks 1–2.
- Produces: a procedure that appends a new DEC block, optionally supersedes a contradicting DEC (bidirectional `Supersedes` / `Superseded by`), and regenerates the status board.

- [ ] **Step 1: Write the file**

````markdown
---
name: dec-creation
description: Create a new DEC (Decision) in DECISIONS.md, checking active DECs for contradictions before adding
user_invocable: true
---

# Create a DEC

Add a new numbered decision to `DECISIONS.md` at the repo root.

## Steps

1. **Preconditions.** Confirm `DECISIONS.md` and `contributors.md` exist at the
   repo root. If either is missing, stop and tell the user to create them first.

2. **Read the request.** Take the decision/work to record from the user's prompt.
   If the prompt is empty, ask the user what the decision is.

3. **Parse the chain.** Read every `## DEC-NNN — …` block in `DECISIONS.md`.
   Compute the next code: `DEC-` + zero-padded (`max(all NNN) + 1`), counting
   every DEC including `Rejected` and `Superseded`. Never reuse a number.

4. **Contradiction scan.** Consider only DECs whose `Status` is NOT `Rejected`
   and NOT `Superseded`. Decide, by reading their `### Decision` sections,
   whether the new request directly contradicts any of them. This is a
   best-effort semantic judgement — surface it, never enforce it silently.

5. **Resolve a contradiction (if any).** Ask the user:
   *"This contradicts DEC-NNN (\<title\>). Supersede it?"*
   - If **no**: stop. Create nothing. Tell the user no DEC was added.
   - If **yes**: on the OLD DEC set `Status: Superseded` and
     `Superseded by: DEC-<new>`; on the NEW DEC set `Supersedes: DEC-<old>`.

6. **Assignee.** Ask who owns it. Validate the `@handle` against the first column
   of the `contributors.md` table. `Unassigned` is allowed. If the handle is not
   listed, offer to add a row to `contributors.md` or pick again.

7. **Body.** Collect `Context` (why), `Decision` (what), and a `Tasks` checklist
   (one or more related tasks). Ask for anything missing.

8. **Branch + date.** Slugify the title to `dec-NNN-slug`; set that as `Branch`.
   Set `Created` to today's date (`YYYY-MM-DD`).

9. **Write.** Append the new DEC block after the last DEC, using this exact shape:

   ```
   ## DEC-NNN — Title

   - Status: Pending
   - Assignee: @handle
   - Created: YYYY-MM-DD
   - Branch: dec-NNN-slug
   - Supersedes: —
   - Superseded by: —
   - Related: —

   ### Context

   …

   ### Decision

   …

   ### Tasks

   - [ ] First task
   ```

   Then regenerate the `## Status board` table: one row per DEC in numeric order,
   columns `| DEC | Title | Status | Assignee |`, including Rejected/Superseded.

10. **Offer the branch.** Ask the user whether to create the git branch now:
    `git checkout -b dec-NNN-slug`. Only run it if they agree.

11. **Confirm.** Report: `Created DEC-NNN — <title>. Status Pending, assignee @x.`
    If a DEC was superseded, mention it.

## Notes

- Do not renumber or reorder existing DECs.
- Commit is the user's call; if asked, use a Conventional Commit subject with no
  AI co-author trailer (see AGENTS.md).
````

- [ ] **Step 2: Verify frontmatter**

Run: `grep -nE '^name: dec-creation$|^user_invocable: true$' .claude/skills/dec-creation/SKILL.md`
Expected: both lines present.

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/dec-creation/SKILL.md
git commit -m "feat: add dec-creation skill"
```

---

### Task 4: `/dec-amend` skill

**Files:**
- Create: `.claude/skills/dec-amend/SKILL.md`

**Interfaces:**
- Consumes: `DECISIONS.md` format and `contributors.md`.
- Produces: a procedure that locates a DEC by code or description and edits its fields/tasks/status/assignee/body, then regenerates the board.

- [ ] **Step 1: Write the file**

````markdown
---
name: dec-amend
description: Amend an existing DEC in DECISIONS.md — find it by code or description, then edit its fields, tasks, status, or assignee
user_invocable: true
---

# Amend a DEC

Edit an existing decision in `DECISIONS.md`.

## Steps

1. **Identify the DEC.** Accept either a DEC code (`DEC-007`) or a free-text
   description. If given a description, search `DECISIONS.md` by title and
   `### Context`/`### Decision` text and confirm the match with the user before
   editing. If nothing matches, say so and stop.

2. **Ask what to change.** Any of: `Context`, `Decision`, the `Tasks` checklist
   (add / edit / tick / untick), `Status` (one of the six), `Assignee`,
   `Related`, or the title. Make exactly the edits the user asks for.

3. **Validate.** If `Status` changes, it must be one of `Pending`, `In Progress`,
   `Blocked`, `Done`, `Rejected`, `Superseded`. If `Assignee` changes, validate
   the `@handle` against `contributors.md` (or `Unassigned`).

4. **Regenerate the status board** so any title/status/assignee change is
   reflected in the top table.

5. **Confirm** the fields that changed.

## Notes

- Do not change the DEC code, and do not renumber or reorder.
- To reject a DEC, prefer the `/dec-deletion` skill. To close one on merge,
  prefer `/dec-close`.
- Commit only if asked; Conventional Commit subject, no AI co-author trailer.
````

- [ ] **Step 2: Verify frontmatter**

Run: `grep -nE '^name: dec-amend$|^user_invocable: true$' .claude/skills/dec-amend/SKILL.md`
Expected: both lines present.

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/dec-amend/SKILL.md
git commit -m "feat: add dec-amend skill"
```

---

### Task 5: `/dec-deletion` skill

**Files:**
- Create: `.claude/skills/dec-deletion/SKILL.md`

**Interfaces:**
- Consumes: `DECISIONS.md` format.
- Produces: a procedure that sets a chosen DEC's `Status` to `Rejected` without renumbering, then regenerates the board.

- [ ] **Step 1: Write the file**

````markdown
---
name: dec-deletion
description: Reject a DEC in DECISIONS.md — sets its status to Rejected without renumbering or reordering the chain
user_invocable: true
---

# Reject a DEC

Mark a decision as rejected. The DEC stays in place; nothing is renumbered.

## Steps

1. **Identify the DEC.** Accept a DEC code (`DEC-007`) or a free-text
   description to search by title/body. Confirm the match with the user. If
   nothing matches, say so and stop.

2. **Confirm intent.** Show the DEC title and current status and confirm the user
   wants to reject it.

3. **Reject.** Set `Status: Rejected` on that DEC. Do NOT delete the block,
   renumber, reorder, or change any other DEC. Optionally note the reason in the
   `### Context` section if the user gives one.

4. **Regenerate the status board** (the DEC keeps its row, now showing
   `Rejected`).

5. **Confirm:** `DEC-NNN — <title> set to Rejected.`

## Notes

- Numbers are never reused; a later DEC still continues from `max+1`.
- Commit only if asked; Conventional Commit subject, no AI co-author trailer.
````

- [ ] **Step 2: Verify frontmatter**

Run: `grep -nE '^name: dec-deletion$|^user_invocable: true$' .claude/skills/dec-deletion/SKILL.md`
Expected: both lines present.

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/dec-deletion/SKILL.md
git commit -m "feat: add dec-deletion skill"
```

---

### Task 6: `/dec-close` skill

**Files:**
- Create: `.claude/skills/dec-close/SKILL.md`

**Interfaces:**
- Consumes: `DECISIONS.md` format and git branch/merge state.
- Produces: a procedure that resolves the DEC from the branch convention, verifies merge to main, sets `Done`, records the merge commit, and regenerates the board.

- [ ] **Step 1: Write the file**

````markdown
---
name: dec-close
description: Close a DEC on merge to main — resolves the DEC from the dec-NNN branch convention, marks it Done, and records the merge commit
user_invocable: true
---

# Close a DEC

Mark a decision Done once its work has merged to `main`.

## Steps

1. **Resolve the DEC.** Read the current git branch (`git rev-parse
   --abbrev-ref HEAD`).
   - If it matches `dec-NNN-*`, that is the DEC.
   - If it is `main`, inspect the most recent merge commit
     (`git log --merges -1 --pretty=%s`) for a `dec-NNN` reference and use it.
   - If still ambiguous, ask the user for the DEC code.

2. **Verify the merge.** Confirm the DEC's branch is merged into `main`
   (`git branch --merged main` lists it, or the merge commit is on `main`). If it
   is not merged yet, warn the user and ask whether to continue anyway.

3. **Open tasks.** If the DEC has unchecked `- [ ]` tasks, list them and ask
   whether to tick them all or leave them. Tick them only with the user's OK.

4. **Close.** Set `Status: Done`. Append a note line under the metadata, e.g.
   `- Merged: <short-sha> on YYYY-MM-DD`, using the merge commit hash and date.

5. **Regenerate the status board** so the DEC shows `Done`.

6. **Confirm:** `DEC-NNN — <title> closed (Done), merged <short-sha>.`

## Notes

- Do not renumber or reorder.
- Commit only if asked; Conventional Commit subject, no AI co-author trailer.
````

- [ ] **Step 2: Verify frontmatter**

Run: `grep -nE '^name: dec-close$|^user_invocable: true$' .claude/skills/dec-close/SKILL.md`
Expected: both lines present.

- [ ] **Step 3: Commit**

```bash
git add .claude/skills/dec-close/SKILL.md
git commit -m "feat: add dec-close skill"
```

---

### Task 7: End-to-end smoke test

**Files:**
- Modify (temporarily): `DECISIONS.md` (via the `/dec-creation` skill)

**Interfaces:**
- Consumes: all four skills and both data files.
- Produces: confidence that a created DEC lands with correct number, board row, and format.

- [ ] **Step 1: Create a throwaway DEC**

Invoke `/dec-creation` with a simple decision (e.g. "Use Node 20 as the version floor"), assignee `@patri`, one task. Decline the branch creation.

- [ ] **Step 2: Verify it landed**

Run: `grep -nE '^## DEC-002 — |^\| DEC-002 ' DECISIONS.md`
Expected: a `DEC-002` block AND a `DEC-002` status-board row, both present.

- [ ] **Step 3: Reject it to confirm the deletion path**

Invoke `/dec-deletion` for `DEC-002`.
Run: `grep -nE '^- Status: Rejected' DECISIONS.md`
Expected: the DEC-002 block shows `Status: Rejected` and its board row reads `Rejected`.

- [ ] **Step 4: Roll back the smoke test**

The throwaway DEC-002 was only to exercise the flow; remove it so the chain starts clean. (Per the no-reuse rule, a real next DEC would still be DEC-003 — note this in the commit if you keep it instead.)

```bash
git checkout -- DECISIONS.md
```

- [ ] **Step 5: Confirm clean state**

Run: `grep -c '^## DEC-' DECISIONS.md`
Expected: `1` (only the bootstrap DEC-001 remains).

---

## Self-Review

**Spec coverage:**
- Single chain file → Task 2. ✓
- Status board → Tasks 2 + every skill regenerates it. ✓
- DEC block format / fields → Task 2 + Task 3 Step 1. ✓
- Six statuses → Global Constraints + Tasks 4/5/6. ✓
- Numbering never reused → Global Constraints + Task 3 Step 3 + Task 7 note. ✓
- contributors.md table, single assignee, validation, Unassigned → Task 1 + Task 3 Step 6. ✓
- `/dec-creation` contradiction scan (active only) + bidirectional supersede + branch offer → Task 3. ✓
- `/dec-amend` find by code/description + edit → Task 4. ✓
- `/dec-deletion` Rejected, no renumber → Task 5. ✓
- `/dec-close` branch convention, fallback, verify merge, tick tasks, record commit → Task 6. ✓

**Placeholder scan:** the `…` inside skill bodies are intentional authoring slots in the skill's *own* template output, not plan gaps; every file's full content is given. No TBD/TODO in plan steps.

**Type consistency:** field names (`Status`, `Assignee`, `Created`, `Branch`, `Supersedes`, `Superseded by`, `Related`), the `## DEC-NNN — Title` heading, and board columns `| DEC | Title | Status | Assignee |` are identical across Tasks 2–7.
